create database cc409_user106;

use cc409_user106;

create table alumno(id_alumno int auto_increment, 
	nombre varchar(40), email varchar(50), usuario varchar(40), 
	contrasena varchar(40), primary key(id_alumno));
